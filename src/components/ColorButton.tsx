import React, { Component } from 'react';

/**
 * Color Array will keep the colors which can be picked. !!!!!Needs to be in its own file
 */
const colorArray = ['#ff0000', '#00ff00', '#0000ff', '#fff000', '#000fff', '#ffa500']

class ColorButton extends Component {
  render() {
    return (
      <div>Hier komt de colorbutton</div>
    );
  }
}

export default ColorButton;